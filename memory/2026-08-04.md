# 2026-08-04

- Updated the local CAIXA database through contest 3039 (2026-08-02) and verified its integrity.
- Upgraded Next.js and `eslint-config-next` to 16.3.0 after `minimumReleaseAge` elapsed; the supply-chain policy was not weakened.
- Replaced timestamp-based analytics cache invalidation with migration 010's monotonic revision and triggers.
- Made image-owned Docker migrations canonical so an older persisted volume cannot hide a new migration.
- Removed production HMAC autogeneration; production now fails closed and E2E supplies an explicit public test value.
- Hardened Docker ownership/capabilities while preserving writable SQLite, logs and Next.js cache paths.
- Consolidated prize backfill on the validated CAIXA client and centralized safe positive-integer CLI parsing.
- Fixed streak-window semantics, `ThemeProvider` hydration/system preference handling and `Button asChild` prop leakage.
- Autoreview found and drove fixes for missing Docker CLI packaging, SSR theme mismatch, writable Next cache, stale system theme and stale-volume migrations. Final pass returned no actionable findings.
- Real browser QA found and fixed a skipped heading level and invalid ARIA labeling in the generator; desktop/mobile remained free of horizontal overflow and console errors.
- Push, staging and production were not performed; each remains behind its required confirmation gate.
