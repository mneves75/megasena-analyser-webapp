# 2026-08-17 — Recuperação de acesso, sync do banco CAIXA e bump do Bun canary

## Acesso ao VPS

- `ssh conhecendotudo` voltou a funcionar depois de o nó Tailscale do VPS
  (`srv849078`, 100.97.0.40) ficar offline com a node key expirada
  (expiry 2026-08-06). Recuperação: console do painel Hostinger →
  `sudo systemctl restart tailscaled && sudo tailscale up` + login interativo.
- O SSH pelo IP público (212.85.2.24) é bloqueado por design: o firewall do VPS
  só aceita Cloudflare. O caminho canônico é sempre o tailnet (`conhecendotudo`).
- Recomendação pendente: desativar key expiry do nó `srv849078` no admin do
  Tailscale para não repetir o incidente.

## Banco de dados

- DB local atualizado via `bun run db:pull -- --limit 20 --incremental`
  (6 sorteios novos; último: #3045 de 2026-08-16; total 3045).
- Upload para o VPS: `scp` do db limpo (sqlite `.backup`) para
  `/root/coolify-migration/compose/megasena-new/db/mega-sena.db`, com backup
  `mega-sena.db.bak-20260817` no servidor.
- Armadilha: ao substituir o db, removi os `-wal`/`-shm` e o diretório
  `/root/.../db` não era gravável pelo usuário do contêiner (`bun` = uid 1000).
  SQLite falhava com "attempt to write a readonly database" porque não
  conseguia recriar o WAL. Fix: `chown ubuntu:ubuntu` no diretório do db.
  Lição: nunca apague `-wal`/`-shm` sem garantir que o diretório é gravável
  pelo usuário do contêiner.

## Runtime

- Bun canary de produção (imagem Docker) atualizado de
  `1.4.0-canary.1+6c12afd8e` para `1.4.0-canary.1+1dd66afde`
  (digest `518322ba…`), imagem `megasena-analyser-app:v1.12.1` construída no
  servidor (Dockerfile runtime-only reaproveita `dist/`).
- Verificado: `/api/health` healthy, 3045 sorteios, container `(healthy)`.

## Domínios

- `conhecendotudo.online` está morto (erro TLS no handshake). O site principal
  migrou para `conhecendotudo.com` e `conhecendotudo.com.br` (Cloudflare, 200).
- O app continua nos próprios domínios: `megasena-analyzer.com` (301),
  `megasena-analyzer.com.br` (200), `megasena-analyzer.online` (301).
- Referências locais defasadas em `docker-compose.traefik.yml`
  (Host `megasena-analyser.conhecendotudo.online`) ainda não foram corrigidas.
