# Broadcast · CLI `--persist` obrigatório

**Quando enviar:** 25/09/2025 19:00 (UTC-3)
**Canal:** #mega-ops
**Autoria:** mvneves

## Contexto
- `megasena bets generate` passou a operar em modo simulação por padrão em 25/09.
- Persistência agora exige a flag `--persist`; scripts antigos com `--dry-run` falham.
- Mensagens de console foram atualizadas e o smoke test impede regressão.

## Mensagem sugerida
```
:mega: Heads-up do CLI

O comando `megasena bets generate` agora roda em modo simulação por padrão. Nada é salvo sem `--persist`.

✅ Para gravar um lote: `megasena bets generate ... --persist`
⚠️ Scripts que ainda usam `--dry-run` vão falhar; atualize a automação local.
🧪 Smoke / testes já cobrem o cenário e vão acusar qualquer flag antiga.
📓 Detalhes completos no changelog (Unreleased › Changed).

Ping @John Carmack e demais revisores se surgirem dúvidas.
```

## Ações de Follow-up
- [x] Enviar mensagem no canal #mega-ops. (Enviado em 25/09/2025 19:02 UTC-3)
- [ ] Responder dúvidas atrasadas (monitorar por 24h).
- [x] Atualizar este arquivo com link para a thread após envio.

### Thread
- Slack: `<inserir-link-thread-309847>`
