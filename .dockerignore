# Dependencies
# NOTE: node_modules is included for API server dependencies (zod, etc.)
# .pnp
.pnp.js

# Build outputs
.next/
out/
build/
dist/
# Note: dist/standalone/ is needed for Docker build (pre-built Next.js)
# Exclude other dist files but keep dist/standalone/
!dist/standalone/
!dist/standalone/**

# Testing
coverage/
.turbo/

# Database files (mount as volume instead)
db/*.db
db/*.db-shm
db/*.db-wal
db/backups/

# Environment files (use docker-compose env instead)
.env
.env.*
!.env.example

# Git
.git/
.gitignore
.gitattributes

# CI/CD
.github/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*~

# OS files
.DS_Store
Thumbs.db

# Secrets (extra safety)
*.pem
*.key
*_rsa*
*_ed25519*
*_ecdsa*
id_*
.secrets*
secrets/
*.secret
*.credentials
credentials.json
auth.json

# Backup files
*.backup
*_backup
*.bak
*.old

# Documentation (not needed in container)
docs/
*.md
!CHANGELOG.md

# Docker files (don't copy into image)
Dockerfile*
docker-compose*.yml
.dockerignore

# Scripts not needed in production
scripts/deploy.sh
scripts/install-ssh-key.sh

# BFG cleanup reports
.bfg-report/
